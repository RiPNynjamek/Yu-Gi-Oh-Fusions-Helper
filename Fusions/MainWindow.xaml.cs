using System.Windows;
using Fusions.ViewModel;

namespace Fusions
{
    /// <summary>
    /// Logique d'interaction pour MainWindow.xaml
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            //MainWindowViewModel vm = new MainWindowViewModel();
            //DataContext = vm;
            InitializeComponent();
        }
    }
}
